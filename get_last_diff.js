const fs = require('fs');
// Wait, I can just check the git log if I init it or just don't care. Since it's a simple video player page, we used `<video controls>` in my code which uses native controls, which is fine!
